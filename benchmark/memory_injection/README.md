# Offline memory-injection benchmark

`run.py` emits a versioned JSON artifact with measured, empty, and degraded
states. Local retrieval work is reported separately from provider-billed usage.
No network or credentials are needed.
