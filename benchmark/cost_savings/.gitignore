# Working outputs regenerated by harness.py (not committed).
# Anchored to this directory so signed evidence under results/ is still tracked.
/out/
/out-live/
/*.db
/qa_journal_*.jsonl
/qa_report_*.json
/hypotheses-*.jsonl
