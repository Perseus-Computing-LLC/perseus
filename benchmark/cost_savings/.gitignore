# Working outputs regenerated by harness.py (not committed).
# Anchored to this directory so historical content-hash evidence under results/ is still tracked.
/out/
/out-live/
/*.db
/qa_journal_*.jsonl
/qa_report_*.json
/hypotheses-*.jsonl
